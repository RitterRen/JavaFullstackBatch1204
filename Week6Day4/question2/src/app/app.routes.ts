import { Routes } from '@angular/router';
import { CityTabsComponent } from './city-tabs/city-tabs.component';

export const routes: Routes = [
    { path: 'city-tabs', component:CityTabsComponent}
];
